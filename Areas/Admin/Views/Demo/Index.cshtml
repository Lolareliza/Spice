
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>




@*@{
        ViewData["Title"] = " Customer ";
        Layout = "~/Views/Shared/_LayoutAccountInventory.cshtml";
    }
*@
<style>
    .body-card {
        margin-left: 2%;
        margin-right: 2%;
        margin-top: 1%;
        margin-bottom: 2%;
    }

    .table-font {
        font-weight: 500;
    }
</style>

<div class="content">
    @*@await Html.PartialAsync("~/Views/Manage/_StatusMessage.cshtml", ViewData["StatusMessage"])*@

    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header body-card">
                    <div class="row">
                        <div class="col-6">
                            <h4 class="card-title">Account Type</h4>
                        </div>
                        <div class="col-6 text-right">
                            <a asp-action="Create" class="btn btn-info btn-md pull-right btn-responsive" data-toggle="modal" data-target="#exampleModalCenter">

                                <span class="btn-label">
                                    <i class="fas fa-plus"></i>
                                </span>

                                Add New Account

                            </a>
                        </div>
                    </div>
                </div>
                <div class="card-body body-card">

                    <div class="table-responsive" style="overflow:auto;">
                        <table id="datatable2" class="table table-hover">
                            <thead>
                                <tr>
                                    <th><strong>S/N</strong></th>
                                    <th><strong>Account Name</strong></th>
                                    <th scope="col"><strong>Account Code</strong></th>
                                    <th scope="col"><strong>Description</strong></th>

                                    <th scope="col" class="disabled-sorting"><strong>Actions</strong></th>
                                </tr>
                            </thead>
                            <tbody class="fontweight">
                                @*@{
                                        int i = 1;

                                        @foreach (var item in Model)
                                        {
                                            <tr>
                                                <td>@i</td>
                                                <td>@item.AccountName</td>
                                                <td>@item.AccountCode</td>

                                                <td>@item.Description</td>

                                                <td class="">
                                                    <a asp-action="EditAccount" asp-route-id="@item.Id" class="btn btn-warning btn-icon btn-sm" data-toggle="tooltip" data-placement="top" title="Click To Edit"><i class="fas fa-edit"></i></a>
                                    <a class="btn btn-success btn-icon btn-sm" asp-action="Details" asp-route-Id="@item.Id"  data-toggle="tooltip" data-placement="top" title="Click To View"><i class="fas fa-list-ul"></i></a>
                                                    <a href="#" class="btn btn-danger btn-icon btn-sm remove" AccountId="@item.Id" AccountName="@item.AccountName" AccountCode="@item.AccountCode" Description="@item.Description" ><i class="fas fa-times" data-toggle="tooltip" data-placement="top" title="Click To Delete"></i></a>
                                                </td>
                                            </tr>

                                            i = i + 1;
                                        }
                                    }
                                *@
                            </tbody>
                        </table>
                        <!-- Button trigger modal -->
                        <!-- Modal -->
                        <div class="modal fade" id="exampleModalCenter" tabindex="" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
                            <div class="modal-dialog modal-dialog-centered" role="document">
                                <div class="modal-content">
                                    <div class="modal-header">
                                        <h5 class="modal-title" id="exampleModalLongTitle">Add an account</h5>
                                        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                            <span aria-hidden="true">&times;</span>
                                        </button>
                                    </div>
                                    <div class="modal-body">
                                        @*
                                        <div class="form-group row">
                                            <div class="col-2">
                                                <label class="col-form-label">Account Name</label>
                                            </div>
                                            <div class="col-5">
                                                <input class="form-control" />
                                            </div>
                                        </div>
                                            *@
                                        <div class="form-inline">
                                            <label class="labeltext col-sm-6">Account Name</label>
                                            <input type="text" class="form-control col-sm-6" required />
                                        </div>
                                        <br />
                                        <div class="form-inline">
                                            <label class="labeltext col-sm-3">Account Code</label>
                                            <input class="form-control col-sm-4" />
                                        </div>

                                        <br />
                                        <div class="form-inline">
                                            <label class="labeltext col-sm-3">Description</label>
                                            <textarea type="text" class="form-control col-sm-4" cols="3"></textarea>
                                        </div>

                                        <br />
                                    </div>
                                    <div class="modal-footer">
                                        <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
                                        <button type="button" class="btn btn-primary">Save</button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>



@section Scripts {

    <script>
        $(document).ready(function () {

            $('#datatable2').DataTable({
                "pagingType": "full_numbers",
                dom: "Bfrtip",
                "lengthMenu": [
                    [10, 25, 50, -1],
                    [10, 25, 50, "All"]
                ],


                buttons: [
                    'copy',
                    'excel',
                    'csv',
                    'pdf',
                    'print',
                ],


                responsive: true,
                language: {
                    search: "_INPUT_",
                    searchPlaceholder: "Search records",
                }

            });

            var table = $('#datatable2').DataTable();


            // Delete a record
            table.on('click', '.remove', function (e) {
                $tr = $(this).closest('tr');

                customerId = $(this).attr('CustomerId');
                customername = $(this).attr('CustomerName');
                email = $(this).attr('Email');
                phone = $(this).attr('Phone');
                address = $(this).attr('Address');
                city = $(this).attr('City');
                zipcode = $(this).attr('ZipCode');
                state = $(this).attr('State');
                country = $(this).attr('Country');
                accountNumber = $(this).attr('AccountNumber');
                bankName = $(this).attr('BankName');
                accountName = $(this).attr('AccountName');
                sortCode = $(this).attr('SortCode');
                website = $(this).attr('Website');
                mobile = $(this).attr('Mobile');
                note = $(this).attr('Note');
                status = $(this).attr('Status');



                swal({
                    title: 'Are you sure?',
                    text: "You are about to delete '" + customername + "'",
                    type: 'warning',
                    showCancelButton: true,
                    confirmButtonClass: 'btn btn-success',
                    cancelButtonClass: 'btn btn-danger',
                    confirmButtonText: 'Yes, delete it!',
                    buttonsStyling: false
                }).then(function () {

                    $.ajax({
                        type: "POST",
                        url: "/Customers/DeleteCustomer",
                        data: JSON.stringify(customerId),
                        dataType: "json",
                        contentType: 'application/json; charset=utf-8',
                        headers: {
                            RequestVerificationToken:
                                $('input:hidden[name="__RequestVerificationToken"]').val()
                        },
                        success: function (r) {

                            if (r.msg == "Success") {

                                swal({
                                    title: 'Deleted!',
                                    text: 'Your file has been deleted.',
                                    type: 'success',
                                    confirmButtonClass: "btn btn-success",
                                    buttonsStyling: false
                                });


                            } else if (r.msg == "Fail") {

                                alert(r.msg);

                            } else if (r.msg == "No Data") {
                                alert(r.msg);

                            } else {
                                alert(r.msg);
                            }
                            //alert(r.msg);
                            // window.location.reload();

                        },
                        error: function () {
                            alert(r.msg);
                        }
                    });


                    table.row($tr).remove().draw();

                }).catch(swal.noop);


                e.preventDefault();
            });

            //Like record
            table.on('click', '.like', function () {
                alert('You clicked on Like button');
            });
        });
    </script>



}


